{% extends 'base.html.twig' %}

{% block title %}Administration Cinéphoria{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            {% for msg in app.session.flashBag.get('success') %}
                <div class="alert alert-success">
                    {{ msg }}
                </div>
            {% endfor %}
                
            {% for msg in app.session.flashBag.get('error') %}
                <div class="alert alert-danger">
                    {{ msg }}
                </div>
            {% endfor %}
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {% if is_granted('ROLE_ADMIN') %}
            <h1 class="">Espace d'Administration</h1>
            {% else %}
            <h1 class="">Espace Intranet</h1>
            {% endif %}
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {% include 'adminspace/navs.html.twig' %}
        </div>
    </div>
    {{ form_start(form, {action: path('app_moviesession_edit', { id:moviesession.id }) }) }}
    <div class="row">
        <div class="col-12">
            <h2 id="titleEquipes">Modification d'une séance</h2>

            {{ form_rest(form) }}
            <input type="hidden" id="csrf_token" value="{{ token }}" />
        </div>
    </div>
    
    <div class="row"><div class="col-12">&nbsp;</div></div>
    
    <div class="row">
        <div class="col-12">
            <input type="submit" class="btn btn-primary" value="Enregistrer" />
            <a class="btn btn-secondary" href="{{ path('app_adminspace_sessions') }}">Annuler</a>
            <button class="btn btn-danger" data-controller="confirmation" data-action="click->confirmation#alert"
                     data-confirmation-url-param="{{ path('app_moviesession_delete', {id: moviesession.id}) }}" 
                     data-confirmation-redirect-param="{{ path('app_adminspace_sessions') }}">Supprimer</button>
        </div>
    </div>
    {{ form_end(form) }}

</div>
{% endblock %}



